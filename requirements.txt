# Pinned to minor versions. Backtest results that cannot be reproduced because
# a dependency changed under you are not results.
pandas>=2.2,<3.1
numpy>=1.26,<3.0
pyarrow>=15.0
requests>=2.31
scipy>=1.11
matplotlib>=3.8
